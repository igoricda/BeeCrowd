#include <stdio.h>
#include <math.h>

int main(void) {

  //Declaração de variáveis
  int dist, tempo;

  //inputs
  scanf("%d", &dist);

  //calc
  tempo = (dist * 2);
  
  //outputs
  printf("%d minutos\n", tempo);

  return 0;
}
